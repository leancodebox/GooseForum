// Naive UI 主题配置
import { darkTheme, lightTheme } from 'naive-ui'


export const light = {
  Select: {
    peers: {
      InternalSelection: {
        textColor: '#FF0000'
      },
      InternalSelectMenu: {
        borderRadius: '6px'
      }
    }
  }
}