This paragraph has **bold text**, _italic text_, ~~deleted text~~, `inline code`,
escaped \*stars\*, and mixed 中文 punctuation.

Second line keeps normal soft break behavior.
Still the same paragraph unless the renderer decides otherwise.
